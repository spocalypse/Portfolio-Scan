# UI instrument reviewer

Review `web/` diffs against SPEC §5.11 and `.cursor/skills/instrument-panel-ui/SKILL.md`.

## Verdict

Ship / Fix before merge / Escalate

Fail if: new hex, radius, shadow, gradient, currency, invented metrics, color-only encoding, or “pretty dashboard” that dilutes divergence.
